# Internal-only broker on the docker network. Harden (auth/TLS) before exposing outside the stack.
persistence true
persistence_location /mosquitto/data/

log_dest stdout

listener 1883
allow_anonymous true
